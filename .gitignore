node_modules
node_modules_failed_pnpm
node_modules_old
package-lock.json
security-reports
packages/design-system/dist

# PR-7a (2026-05): generated declaration files must NOT live under src/.
# tsconfig.build.json correctly emits to dist/, but historic build runs
# left 619 .d.ts artifacts committed under src/. Removed in PR-7a; this
# pattern prevents them from re-entering the tree if a stray local build
# misconfigures emission.
packages/design-system/src/**/*.d.ts
packages/design-system/src/**/*.d.ts.map

*storybook.log
storybook-static
apps/docs/.next/
apps/docs/out/

# Evidence registry (generated)
.evidence/
apps/mfe-shell/.env.local

# Module Federation temp files
.__mf__temp/
.mf/

# OS artifacts
.DS_Store
Thumbs.db

# Editor temp files
*.swp
*.swo
*~

# Backup files
*.bak

# Stryker mutation testing temp
.stryker-tmp/

# Archive (migrated, no longer needed)
archive/
public/design-tokens.source.json
web/.eslintcache
apps/*/dist/
