// @tailwindcss/postcss no longer needed — TW4 CSS processed by @tailwindcss/webpack loader
// PostCSS config retained for AG Grid and other non-TW CSS that may need PostCSS plugins
module.exports = {
  plugins: {},
};
