{
  "_description": "STRICT_GATES=false (default). Both advisory jobs broken. Aggregator passes — advisory failures only block when strict mode is on. This is the pre-cutover default.",
  "_expected_exit": 0,
  "strict_gates": "false",
  "required": {
    "unit": { "result": "success" },
    "token_drift": { "result": "success" },
    "cssom_canary": { "result": "success" },
    "visual_invariant": { "result": "success" }
  },
  "advisory": {
    "cssom_full": { "result": "success", "outcome": "failure" },
    "lint": { "result": "failure", "outcome": "skipped" }
  }
}
