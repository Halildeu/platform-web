node_modules
dist
.next
coverage
*.min.js
*.min.css
pnpm-lock.yaml
apps/docs/.next

# Nginx CSP must remain one physical line. Generic prose wrapping inserts a
# newline into the emitted HTTP header and makes the response malformed.
apps/etik-speak-public/security-headers.conf.inc
apps/etik-speak-manager/security-headers.conf.inc

# Generated artifacts — must round-trip through their generator script
# unchanged. Prettier formatting (e.g. inline-arrays) drifts from the
# raw JSON.stringify output and breaks the freshness check.
apps/mfe-shell/src/app/manifest/page-manifest.schema.json

# Faz 21.8 PR-X5: x-charts adoption matrix is generated by
# `scripts/ci/x-charts-adoption-scan.mjs`. Prettier markdown formatting
# (escape sequences, table alignment) breaks the `--check` freshness gate.
docs/x-charts-adoption-matrix.md
docs/x-charts-adoption-matrix.json

# Design system token build artifacts — emitted by build-tokens.mjs (and
# legacy generators for token-types.ts + docs.json). Prettier reformats
# CSS pseudo-class selectors (`focus-visible:` → `focus-visible: `), strips
# semantic underscores inside `color-mix()`, and switches `"x"` → `'x'`
# quote style; all break round-trip with the generator output.
packages/design-system/src/tokens/build/

# Exact-owned theme generator outputs. Their byte representation is the
# freshness contract checked by `pnpm run tokens:drift`; formatter mutation
# would make a freshly generated artifact stale in the same commit.
apps/mfe-shell/src/styles/theme.css
apps/mfe-shell/src/styles/generated-theme-inline.css
apps/mfe-shell/src/styles/component-theme.generated.css
design-tokens/generated/theme-contract.json
design-tokens/generated/token-types.ts
