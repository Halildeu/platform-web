/**
 * Chromatic workflow trigger file.
 * Intentional no-op; ensures `stories/**` path changes to fire CI.
 * Updated to retrigger Chromatic after workflow fix.
 */
export const chromaticTrigger = true;
